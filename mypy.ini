[mypy]
# Exclude archived test files to prevent duplicate module name errors.
# The archive_sprint1/ directory contains copies of test files from a
# previous sprint; mypy sees them as duplicate modules when scanning
# the entire .claude/hooks/ tree.
exclude = archive_sprint1
