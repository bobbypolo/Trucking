"""Smoke tests for .claude/hooks/ modules.

This package is intentionally minimal — it exists so pytest can discover
.claude/hooks/tests/test_*.py files. See test_smoke.py for the import
checks that run in the python-hooks CI job.
"""
